// Test stub for Next.js `server-only` package.
// The real module throws if imported into a client bundle; in unit tests
// (plain Node) there is no such concern, so this is an intentional no-op.
export {}
